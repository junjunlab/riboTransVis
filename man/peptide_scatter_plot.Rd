% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiPeptide-occupancy.R
\name{peptide_scatter_plot}
\alias{peptide_scatter_plot}
\title{Scatter Plot of Peptide Pause Scores}
\usage{
peptide_scatter_plot(
  data = NULL,
  x = NULL,
  y = NULL,
  color = "#003366",
  label_size = 3,
  top_motif = 20
)
}
\arguments{
\item{data}{A \code{data.frame} containing peptide sequences and their pause scores
in different sample conditions.}

\item{x}{A character string specifying the column name for the \strong{X-axis condition}.}

\item{y}{A character string specifying the column name for the \strong{Y-axis condition}.}

\item{color}{A string defining the \strong{scatter color} (default: \code{"#003366"}).}

\item{label_size}{A numeric value controlling the labeled peptide text size
(default: \code{3}).}

\item{top_motif}{An integer specifying the number of top peptides to highlight based
on \strong{highest fold-change (\code{y / x})} (default: \code{20}).}
}
\value{
A \strong{ggplot2 scatter plot} comparing \code{x} vs. \code{y} peptide density values.
The top \code{top_motif} peptides with \strong{largest fold-change} are labeled.
}
\description{
This function generates a scatter plot comparing ribosome pause scores
between two sample conditions, highlighting the \strong{top N peptides} with
the highest ratio difference.
}
\details{
The function plots each peptide's ribosome occupancy in two conditions
(specified as \code{x} and \code{y}). It highlights the \code{top_motif} peptides with
the \strong{largest ratio difference (\code{y / x})}, labeling them on the plot.
A \strong{dashed y = x line} is included as a reference.
}
\examples{
\dontrun{
  # Generate scatter plot comparing wt_rep1 vs sgeIF5A_rep1
  peptide_scatter_plot(
    data = tripep_pause_score_wide,
    x = "wt_rep1",
    y = "sgeIF5A_rep1",
    color = "#009933",
    top_motif = 5
  )
}

}

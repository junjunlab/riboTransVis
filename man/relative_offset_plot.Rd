% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization-QC.R
\name{relative_offset_plot}
\alias{relative_offset_plot}
\alias{relative_offset_plot,ribotrans-method}
\title{Generic function for plotting relative offset in ribosome profiling data}
\usage{
relative_offset_plot(object, ...)

\S4method{relative_offset_plot}{ribotrans}(
  object,
  type = c("rel2start", "rel2stop"),
  read_length = c(20, 35),
  rel_dist = c(-30, 30),
  rm_yaxis_label = TRUE,
  max_offset_labelx = 0.9,
  max_offset_labely = 0.9,
  return_offset = FALSE
)
}
\arguments{
\item{object}{A \code{ribotrans} object containing ribosome profiling data.}

\item{...}{Additional arguments passed to specific methods.}

\item{type}{Character string specifying the reference point: \code{"rel2start"} for
start codon or \code{"rel2stop"} for stop codon. Default is \code{"rel2start"}.}

\item{read_length}{A numeric vector of length two specifying the range of read
lengths to include (default: \code{c(20, 35)}).}

\item{rel_dist}{A numeric vector of length two specifying the relative distance
range to include (default: \code{c(-30, 30)}).}

\item{rm_yaxis_label}{Logical; if \code{TRUE}, removes y-axis labels (default: \code{TRUE}).}

\item{max_offset_labelx}{Numeric value (between 0 and 1) specifying the x position
of the label showing the most frequent offset (default: \code{0.9}).}

\item{max_offset_labely}{Numeric value (between 0 and 1) specifying the y position
of the label showing the most frequent offset (default: \code{0.9}).}

\item{return_offset}{Logical; if \code{TRUE}, returns the most frequently observed
relative offsets instead of plotting (default: \code{FALSE}).}
}
\value{
A plot showing the relative offset of reads.

If \code{return_offset = FALSE}, returns a \code{ggplot} object with
the relative offset plot. If \code{return_offset = TRUE}, returns a data frame
containing the most frequently observed offsets.
}
\description{
This generic function is used to plot the relative offset of reads
in ribosome profiling data.

This method generates a plot of relative offsets of ribosome profiling
reads mapped to either the start or stop codon.
}
\details{
This function visualizes the distribution of ribosome footprint
offsets relative to the start or stop codon. The plot includes read length on
the y-axis and relative position on the x-axis. The most frequent offset positions
are labeled, and vertical dashed lines highlight expected P-site positions.
}
\examples{
\dontrun{
  # Generate summary information before plotting
  ribo_obj <- generate_summary(ribo_obj)

  # Plot relative offsets with default parameters
  relative_offset_plot(ribo_obj)

  # Get the most frequent offset information
  offsets <- relative_offset_plot(ribo_obj, return_offset = TRUE)
}

}
\seealso{
\code{\link{relative_offset_plot,ribotrans-method}}

\code{\link{generate_summary}}
}

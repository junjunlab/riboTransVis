% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relative-motif-occupancy.R
\name{relative_motif_plot}
\alias{relative_motif_plot}
\title{Plot Relative Motif Occupancy from Ribo-seq Data}
\usage{
relative_motif_plot(data = NULL, mode = c("nt", "codon"))
}
\arguments{
\item{data}{A \code{data.frame} containing the processed ribosome occupancy data.
It must include the following columns:
\itemize{
\item \code{dist} - The relative nucleotide distance from the motif.
\item \code{avg_val} - The average normalized ribosome occupancy.
\item \code{sample} - The sample name.
\item \code{motif} - The amino acid motif.
}}

\item{mode}{Character string specifying the unit for the x-axis:
\itemize{
\item \code{"nt"}: Nucleotide level resolution (default).
\item \code{"codon"}: Codon-level resolution.
}}
}
\value{
A \code{ggplot2} object visualizing ribosome occupancy relative to motifs.
The plot includes:
\itemize{
\item A \strong{line plot} (\code{geom_path}) of ribosome occupancy (\code{avg_val}) across nucleotide positions (\code{dist}).
\item A \strong{facet grid} (\code{facet_grid(motif ~ sample)}) to display results separately for each motif/sample.
\item Custom \strong{theme} settings for improved visualization.
}
}
\description{
This function generates a plot of ribosome occupancy relative to specific
amino acid motifs identified in coding sequences (CDS) using Ribo-seq data.
}
\details{
This function takes as input the output from \code{relative_motif_occupancy()}, which
contains motif-centered ribosome occupancy profiles, and visualizes them in a grid format
where each row represents a different motif and each column represents a different sample.
}
\examples{
\dontrun{
result <- relative_motif_occupancy(object = ribo_data, cds_fa = "cds_sequences.fasta",
                                   motif_pattern = c("PPP", "PPAP"), motif_upstream = 100,
                                   motif_downstream = 100, min_counts = 50)

# Generate the occupancy plot
relative_motif_plot(result)
}

}

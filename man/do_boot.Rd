% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{do_boot}
\alias{do_boot}
\title{Bootstrap confidence intervals for per-column statistics}
\usage{
do_boot(
  mat = NULL,
  boot_n = 1000,
  method = c("median", "mean", "sum"),
  conf = 0.95
)
}
\arguments{
\item{mat}{Numeric matrix of dimension \code{n_samples} × \code{n_positions}.
Each row is one independent sample (e.g. a gene), each column a position.}

\item{boot_n}{Integer - number of bootstrap replicates. Defaults to 1000.}

\item{method}{Character; one of \code{"median"}, \code{"mean"} or \code{"sum"}.
Defaults to \code{"median"}.}

\item{conf}{Numeric in (0,1); confidence level. E.g. \code{0.95} for 95\% CIs.}
}
\value{
A data frame with three columns:
\describe{
\item{rel}{Position identifier from column names}
\item{ci_low}{Lower confidence interval bound}
\item{ci_high}{Upper confidence interval bound}
}
}
\description{
Calculate bootstrap confidence intervals for position-wise summary statistics
}
\details{
The bootstrap procedure works as follows:
\enumerate{
\item Generate \code{boot_n} bootstrap samples by resampling rows with replacement
\item Calculate column-wise statistics for each bootstrap sample
\item Compute quantiles from bootstrap distribution
}
}
\examples{
\dontrun{
mat <- matrix(rnorm(1000), nrow = 100, dimnames = list(NULL, 1:10))
do_boot(mat, boot_n = 1000, method = "median")
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization-QC.R
\name{relative_dist_plot}
\alias{relative_dist_plot}
\alias{relative_dist_plot,ribotrans-method}
\title{Generate Relative Distance Plot}
\usage{
relative_dist_plot(object, ...)

\S4method{relative_dist_plot}{ribotrans}(
  object,
  return_data = FALSE,
  type = c("rel2start", "rel2stop"),
  read_length = c(20, 35),
  rel2st_dist = c(-50, 100),
  rel2sp_dist = c(-100, 50),
  facet_wrap = ggh4x::facet_wrap2(~sample, scales = "free"),
  facet_grid = ggh4x::facet_grid2(qwidth ~ sample, scales = "free", independent = "y"),
  collapse_readlength = FALSE
)
}
\arguments{
\item{object}{A \code{ribotrans} object containing summary information.}

\item{...}{Additional arguments passed to class-specific methods.}

\item{return_data}{Whether return the data for plot, default \code{FALSE}.}

\item{type}{Character string specifying the plot type. Must be one of:
\itemize{
\item \code{"rel2start"} - Relative to the start codon.
\item \code{"rel2stop"} - Relative to the stop codon.
}}

\item{read_length}{Numeric vector of length two specifying the filter range for read length (default: \code{c(20,35)}).}

\item{rel2st_dist}{Numeric vector of length two specifying the relative distance range to the start codon (default: \code{c(-50,100)}).}

\item{rel2sp_dist}{Numeric vector of length two specifying the relative distance range to the stop codon (default: \code{c(-100,50)}).}

\item{facet_wrap}{A \code{ggh4x::facet_wrap2()} object for facetting by sample (default behavior).}

\item{facet_grid}{A \code{ggh4x::facet_grid2()} object for facetting by read length and sample.}

\item{collapse_readlength}{Logical, whether to collapse the visualization across all read lengths (\code{TRUE}) or show separate read lengths (\code{FALSE}, default).}
}
\value{
A \code{ggplot2} object representing the relative distance distribution of reads.

A \code{ggplot2} object visualizing the relative distribution to start/stop codons by frame.
}
\description{
This generic function generates a relative distance distribution plot for sequencing reads.

This method generates a plot showing the relative distance of reads to either
the start codon or stop codon, grouped by frame.
}
\details{
\itemize{
\item If \code{type = "rel2start"}, the distance is calculated as \code{pos - mstart} (relative to start codon).
\item If \code{type = "rel2stop"}, the distance is calculated as \code{pos - mstop} (relative to stop codon).
\item Filtering is applied based on \code{read_length}, \code{rel2st_dist}, and \code{rel2sp_dist}.
\item The plot can either group data by read length (\code{collapse_readlength = FALSE}) or aggregate reads (\code{collapse_readlength = TRUE}).
}
}
\section{Methods (by class)}{
\itemize{
\item \code{relative_dist_plot(ribotrans)}: Generate a relative distance plot for \code{ribotrans} objects.

}}
\examples{
\dontrun{
# Generate relative plot with start codon distances
relative_dist_plot(ribo_obj, type = "rel2start")

# Generate relative plot with stop codon distances
relative_dist_plot(ribo_obj, type = "rel2stop")
}

}

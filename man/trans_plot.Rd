% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization-methods.R
\name{trans_plot}
\alias{trans_plot}
\alias{trans_plot,ribotrans-method}
\title{Visualize transcript signals}
\usage{
trans_plot(object, ...)

\S4method{trans_plot}{ribotrans}(
  object,
  selected_id = NULL,
  type = c("ribo", "rna", "ribo_rna", "scaled_ribo"),
  layer = c("line", "col"),
  facet_layer = ggplot2::facet_grid(sample ~ rname, switch = "y"),
  new_signal_range = TRUE,
  range_x = 0.9,
  range_y = 0.9,
  range_size = 4,
  range_digit = 1,
  scale_factor = 1,
  utr_width = 1,
  cds_width = 3,
  utr_col = "grey",
  cds_col = "grey30",
  nrow = NULL,
  ncol = NULL
)
}
\arguments{
\item{object}{An S4 object of class \code{ribotrans}, containing ribosome profiling data, RNA-seq data, and gene annotation features.}

\item{...}{Additional parameters to be used in specific method implementations.}

\item{selected_id}{A character vector of specific transcript IDs to plot. Defaults to \code{NULL}, where all transcripts associated with the gene are plotted.}

\item{type}{A character string specifying the type of plot to generate.
Possible values are:
\itemize{
\item \code{"ribo"}: Ribosome footprint data.
\item \code{"rna"}: RNA-seq data.
\item \code{"ribo_rna"}: Ribosome occupancy normalized by RNA-seq (scaled by \code{scale_factor}).
\item \code{"scaled_ribo"}: Scaled ribosome occupancy over RNA.
}}

\item{layer}{A character string defining the plot layer. Options are:
\itemize{
\item \code{"line"}: Line plot.
\item \code{"col"}: Bar (column) plot.
}}

\item{facet_layer}{ggplot2 faceting specification, default facet_grid(sample~rname,switch = "y").}

\item{new_signal_range}{Logical. If \code{TRUE}, adds a signal range annotation to the plot. Default: \code{TRUE}.}

\item{range_x}{Numerics specifying the x position of the signal range annotation
in normalized parent coordinates \verb{[0, 1]}. Default: \code{0.9}.}

\item{range_y}{Numerics specifying the y position of the signal range annotation
in normalized parent coordinates \verb{[0, 1]}. Default: \code{0.9}.}

\item{range_size}{Numeric. Text size for the signal range label. Default: \code{4}.}

\item{range_digit}{Numeric. Number of decimal places for rounding signal range values. Default: \code{1}.}

\item{scale_factor}{Numeric, a scaling factor for RNA-seq reads when \code{type = "ribo_rna"} (default = 1.0).}

\item{utr_width}{Numeric, the line width for untranslated regions (UTRs) in the transcript structure (default = 1.0).}

\item{cds_width}{Numeric, the line width for coding sequences (CDS) in the transcript structure (default = 3.0).}

\item{utr_col}{Character, the color to use for UTR regions (default = "grey").}

\item{cds_col}{Character, the color to use for CDS regions (default = "grey30").}

\item{nrow}{Integer, the number of rows for arranging plots in a grid layout (default = \code{NULL}, auto-calculated).}

\item{ncol}{Integer, the number of columns for arranging plots in a grid layout (default = \code{NULL}, auto-calculated).}
}
\value{
A combined ggplot object or a plot grid as generated by \code{cowplot::plot_grid}, representing transcript plots with annotations.
}
\description{
Generic function definition for visualizing transcript-level signals, including ribosome occupancy and RNA-seq coverage.

Method implementation for \code{trans_plot}, specifically for objects of class \code{ribotrans}.
}
\details{
This method facilitates visualizing transcript-level data from ribosome footprint profiling and RNA-seq analyses. The \code{type} parameter enables viewing raw ribosome or RNA data and scaled metrics. Annotations, such as UTR and CDS regions, are included for transcript-specific structural context.

The plots are arranged in facets by sample and transcript ID, and plotting options (lines or bars) are easily customizable via the \code{layer} parameter.
}
\seealso{
\code{\link{trans_plot,ribotrans-method}} for the method implementation specific to the \code{ribotrans} class.

\code{\link{trans_plot}} for the generic function definition.
}

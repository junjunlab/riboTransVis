% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generic-methods.R
\name{get_occupancy,ribotrans-method}
\alias{get_occupancy,ribotrans-method}
\title{Extract Ribosomal Footprint Occupancy for a Gene}
\usage{
\S4method{get_occupancy}{ribotrans}(
  object,
  gene_name = NULL,
  smooth = c("TRUE", "FALSE"),
  slide_window = 30
)
}
\arguments{
\item{object}{A \code{ribotrans} object containing ribosome profiling data, BAM files, and total read counts.}

\item{gene_name}{A \code{character} string specifying the gene of interest.}

\item{smooth}{A \code{character} string (\code{"TRUE"} or \code{"FALSE"}, default: \code{"TRUE"})
indicating whether to apply smoothing to occupancy values.}

\item{slide_window}{An \code{integer} specifying the window size used for smoothing (default: \code{30}).}
}
\value{
A \code{ribotrans} object with updated slots:
\describe{
\item{ribo_occupancy}{A \code{data.frame} with per-position ribosome occupancy.}
\item{ribo.smoothed}{A \code{character} (\code{"TRUE"}/\code{"FALSE"}) marking if smoothing was applied.}
\item{gene_name}{Updated with the queried \code{gene_name}.}
}
}
\description{
Computes per-position ribosome occupancy from a \code{ribotrans} object.
Optionally, a \strong{sliding window smoothing} may be applied.
}
\details{
This method extracts \strong{ribosome footprint occupancy per transcript position} using \code{getOccupancy()}.
\strong{Workflow:}
\enumerate{
\item Filters \strong{ribo-seq BAM files} associated with the \code{ribotrans} object.
\item Matches \strong{total mapped reads} for normalization (RPM).
\item Iterates over \strong{all ribo-seq samples}, computing per-position occupancy.
\item \strong{Optional smoothing} using \code{smoothEachPosition()}, applying a sliding window.
}

\strong{Reads Per Million (RPM) Normalization:}
\deqn{RPM = \frac{\text{Read Count at Position}}{\text{Total Mapped Reads}} \times 10^6}
}

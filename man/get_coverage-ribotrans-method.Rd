% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generic-methods.R
\name{get_coverage,ribotrans-method}
\alias{get_coverage,ribotrans-method}
\title{Extract RNA-Seq Read Coverage for a Gene}
\usage{
\S4method{get_coverage}{ribotrans}(
  object,
  gene_name = NULL,
  smooth = c("TRUE", "FALSE"),
  slide_window = 30
)
}
\arguments{
\item{object}{A \code{ribotrans} object containing transcript annotations, BAM files, and library size.}

\item{gene_name}{A \code{character} string specifying the gene of interest.}

\item{smooth}{A \code{character} string (\code{"TRUE"} or \code{"FALSE"}, default: \code{"TRUE"})
indicating whether to apply smoothing.}

\item{slide_window}{An \code{integer} specifying the window size used for smoothing (default: \code{30}).}
}
\value{
A \code{ribotrans} object with updated slot:
\describe{
\item{RNA_coverage}{A \code{data.frame} with per-position read coverage.}
\item{RNA.smoothed}{A \code{character} (\code{"TRUE"}/\code{"FALSE"}) denoting if smoothing was applied.}
\item{gene_name}{Updated with \code{gene_name} argument.}
}
}
\description{
Retrieves per-position RNA-seq read coverage from a \code{ribotrans} object.
Normalization and optional smoothing are applied.
}
\details{
The function \strong{retrieves RNA-seq coverage per nucleotide position} using \code{getCoverage()}.
The process involves:
\enumerate{
\item Extracting \strong{RNA-seq BAM files} from the \code{ribotrans} object.
\item Matching \strong{library size (total mapped reads)} for each sample.
\item Iterating over samples to compute per-position coverage.
\item \strong{Optional smoothing} using \code{smoothEachPosition()}, applying a sliding window.
}

\strong{Smoothing is done with a sliding mean filter over transcript positions.}
}

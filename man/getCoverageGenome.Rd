% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/implementation-genome.R
\name{getCoverageGenome}
\alias{getCoverageGenome}
\title{Compute RNA-seq Coverage on the Genome}
\usage{
getCoverageGenome(
  bam_file = NULL,
  gene_name = NULL,
  features = NULL,
  total_reads = NULL,
  coordinate_to_trans = FALSE
)
}
\arguments{
\item{bam_file}{Character. Path to the RNA-seq BAM file.}

\item{gene_name}{Character. The gene name for which transcript coverage is extracted.}

\item{features}{A \code{data.frame} or \code{GRanges} object containing transcript annotation.}

\item{total_reads}{Numeric. The total number of mapped reads (for RPM normalization).}

\item{coordinate_to_trans}{Logical. Whether to convert genomic coordinates to transcript
coordinates. Default is FALSE.}
}
\value{
A \code{data.frame} with transcript coverage information:
\itemize{
\item \code{rname}: Transcript and gene identifier (\code{"transcript_id|gene_name"}).
\item \code{pos}: Transcriptomic position of the read coverage.
\item \code{count}: Coverage (number of reads covering the position).
\item \code{rpm}: Reads per million (RPM) normalized coverage.
}
}
\description{
The \code{getCoverageGenome()} function extracts read coverage information from a BAM file
for a given gene, converts it to transcriptomic positions, and calculates normalized
coverage (RPM).
}
\examples{
\dontrun{
bam_file <- "example.bam"
gene <- "TP53"
total_mapped_reads <- 1e7
features <- prepareTransInfo_forGenome(gtf_file = "example.gtf")

coverage_data <- getCoverageGenome(
  bam_file = bam_file,
  gene_name = gene,
  features = features,
  total_reads = total_mapped_reads
)
head(coverage_data)
}

}

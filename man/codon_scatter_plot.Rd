% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codon-occupancy.R
\name{codon_scatter_plot}
\alias{codon_scatter_plot}
\title{Codon or Amino Acid Occupancy Scatter Plot}
\usage{
codon_scatter_plot(
  codon_data = NULL,
  x = NULL,
  y = NULL,
  type = c("codon", "amino"),
  codon_labels = NULL,
  color = "#FF6600",
  size = 2
)
}
\arguments{
\item{codon_data}{A \code{data.frame} or \code{tibble} containing occupancy data. Should include columns \code{sample}, \code{codon_seq} (for codon analysis), \code{Abbreviation1} (for amino acid analysis), and \code{reloccup}.}

\item{x}{Character. Sample name to be shown on the x-axis (must match one of the sample names in \code{codon_data}).}

\item{y}{Character. Sample name to be shown on the y-axis (must match one of the sample names in \code{codon_data}).}

\item{type}{Character. Type of sequence to analyze: either \code{"codon"} or \code{"amino"}. Default is \code{"codon"}.}

\item{codon_labels}{Character vector. Optional. A vector of codon triplets or one-letter amino acid codes to label in the plot.}

\item{color}{Character. Color code for points (default: \code{"#FF6600"}).}

\item{size}{Numeric. Size of points in the scatter plot (default: 2).}
}
\value{
A \code{ggplot2} object of the scatter plot, showing codon or amino acid occupancy in two conditions.
}
\description{
Generate a scatter plot comparing codon or amino acid occupancy between two samples.
Supports optional labeling of specific codons/amino acids, with identity line and correlation statistics.
}
\details{
Function workflow:
\itemize{
\item For type = "codon": reshapes \code{codon_data} to wide format and plots codon-level scatter plot.
\item For type = "amino": averages occupancy per amino acid and plots amino acid-level scatter plot.
\item Optionally labels selected codons or amino acids.
\item Includes reference y = x dashed line and Pearson correlation coefficient (via \code{ggpubr::stat_cor}).
}
}
\examples{
\dontrun{
# For codon-level comparison
codon_scatter_plot(codon_data = df, x = "wt", y = "mut", type = "codon",
                   codon_labels = c("AAA", "GGG"))

# For amino acid comparison
codon_scatter_plot(codon_data = df, x = "ctrl", y = "treat", type = "amino")
}

}
